# Required: 0x-prefixed 64-char hex private key (32 bytes)
# Example format: 0x followed by exactly 64 hex characters (0-9, a-f)
IVXP_PRIVATE_KEY=0xYOUR_64_HEX_CHARS_HERE

# Optional: "base-sepolia" (default) or "base-mainnet"
IVXP_NETWORK=base-sepolia

# Optional: Provider URL (only needed when running client.ts standalone)
IVXP_PROVIDER_URL=http://localhost:3001
